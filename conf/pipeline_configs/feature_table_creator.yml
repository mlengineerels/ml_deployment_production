input_table: 'delta_db.booking'
data_prep_params:
  label_col: 'booking_status'
  ohe: False
  # if ohe->  one hot encoding is true mention the cat colmns
  # cat_cols: ['type_of_meal', 'room_type', 'market_segment_type', 'date_of_reservation']
  
  drop_missing: False
