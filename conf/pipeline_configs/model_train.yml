mlflow_params:
  run_name: 'random_forest_baseline'

pipeline_params:
  test_size: 0.20
  random_state: 42

model_params:
  n_estimators: 100
  max_depth: 12

  min_samples_leaf: 1
  max_features: 10
  random_state: 42